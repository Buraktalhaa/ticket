generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URI")
}

model user {
  id            String      @id @default(uuid())
  email         String      @unique
  password      String
}
