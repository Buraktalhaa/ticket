generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URI")
}

// postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA

model Auth {
  id           String @id @default(uuid())
  email        String @unique
  name         String
  password     String
  token        Token? 

  User User?
}

model Token {
  id            String   @id @default(uuid())
  accessToken   String
  refreshToken  String
  authId        String   @unique
  auth          Auth     @relation(fields: [authId], references: [id])

  createdAt     DateTime @default(now())
}


model User {
  id       String  @id @default(uuid())
  name     String
  surname  String
  email    String  @unique
  birthday String
  active   Boolean @default(true)

  authId String @unique
  auth   Auth   @relation(fields: [authId], references: [id])
  Card   Card[]
}

model Card {
  id              String @id @default(uuid())
  userId          String
  cardInformation String @db.Text
  user            User   @relation(fields: [userId], references: [id])
}

model Permission {
  id         String  @id @default(uuid())
  userId     String
  roleId     String
  role   Role    @relation(fields: [roleId], references: [roleId])
}

enum RoleType {
  USER
  SELLER
  ADMIN
}

model Role {
  roleId      String       @id @default(uuid())
  roleType    RoleType
  permissions Permission[]
}

// Auth Table
// id, email, hashedPassword, refreshToken

// User Table
// userId, name, surname, email, birthday, picture, active

// Card Table
// id, userId, cardInformation, user

// Permission Table
// id, userID, roleId, user, role

// enum RoleType
// Role Table
// roleId, roleType RoleType, permissions Permission[]
